import { FC } from 'react';
import { Card } from 'antd';

type MaterialShopProps = {}
const MaterialShop: FC = (props: MaterialShopProps) => {
  return (
    <Card>
      MaterialShop #END
    </Card>
  );
};

export default MaterialShop;
